jQuery(document).ready(function ($) {
    const elements = document.getElementsByClassName("js-post-notice");
    while(elements.length > 0){
        elements[0].parentNode.removeChild(elements[0]);
    }
})
